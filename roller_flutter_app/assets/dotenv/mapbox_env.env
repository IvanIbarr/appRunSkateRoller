# Mapbox — alineado con RN (`REACT_APP_MAPBOX_ACCESS_TOKEN` en appRunSkateRoller/src/config/mapbox.ts).
# Chrome/Web: este asset se carga con flutter_dotenv; el `.env` en disco no (navegador).
# Para Web usa: flutter run -d chrome --dart-define-from-file=mapbox.local.env
#
# Descomenta UNA línea con tu pk. real (no subas tokens a git público):
#REACT_APP_MAPBOX_ACCESS_TOKEN=pk.eyJ1Ijo...
#MAPBOX_ACCESS_TOKEN=pk.eyJ1Ijo...
