# Copia a .env y ajusta. Bitácora en carpeta logs/ (combined-YYYY-MM-DD.log, error-YYYY-MM-DD.log)
# Rotación diaria; archivos con más de 3 días se eliminan automáticamente.

# Nivel: error | warn | info | debug | silly
# LOG_LEVEL=info

# Ingesta de logs desde la app (web/native) → POST /api/logs/client
# - Por defecto: activa si NODE_ENV no es production; en producción desactivada salvo que pongas true.
# CLIENT_LOG_INGEST=true
# CLIENT_LOG_INGEST=false

# Opcional: si defines secreto, el cliente debe enviar cabecera X-Client-Log-Secret (DefinePlugin REACT_APP_CLIENT_LOG_SECRET en web).
# CLIENT_LOG_SECRET=
